import {Color} from '../../types/typing';

// Only use/add colors as per Figma(Design) style guide.
// Use the same color names and color codes as in Figma(Design) style guide.
const colors: Color = {
  primary: '#3E66FB',
  none: '#ffffff',
  default: '#000000',
  grey: '#ADB5BD',
  success: '#9DE09D',
  error: '#F03738',
};

export default colors;
